rules:
  - id: avoid-eval
    patterns:
      - pattern: eval(...)
    message: "Avoid using eval(). It's dangerous."
    severity: ERROR
    languages: [python]
    metadata:
      owasp: A1-Injection
